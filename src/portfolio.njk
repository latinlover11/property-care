---
title: "Our Work"
description: "Browse before-and-after photos of Property Care projects across Boulder County and beyond."
ogUrl: "portfolio.html"
root: ""
active: "portfolio"
---
<!DOCTYPE html>
<html lang="en">
<head>
  {% include "head.njk" %}
</head>
<body>
<a href="#main" class="skip-link">Skip to main content</a>

{% include "icons.njk" %}
{% include "nav.njk" %}

  <main id="main">

    <!-- PAGE HERO -->
    <section class="page-hero" style="position:relative; overflow:hidden;">
      <div style="position:absolute; inset:0; background:linear-gradient(135deg, rgba(61,43,31,0.45) 0%, transparent 100%); pointer-events:none;"></div>
      <div style="position:absolute; inset:0; opacity:0.04; background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E"); background-size:256px; pointer-events:none;"></div>
      <div class="section" style="padding-top:0; padding-bottom:0; position:relative; z-index:1;">
        <div class="breadcrumb">
          <a href="/">Home</a>
          <span class="sep">/</span>
          <a href="/portfolio">Portfolio</a>
          <span class="sep">/</span>
          <a href="/service-areas">Service Areas</a>
          <span class="sep">/</span>
          <a href="/">Home</a>
        </div>
        <p class="section-label" style="font-size:0.82rem; letter-spacing:0.1em;">Our Work</p>
        <h1 class="section-title">Browse before-and-after photos of Property Care projects across Boulder County and beyond.</h1>
        <div style="width:40px; height:3px; background:var(--clay); border-radius:2px; margin-top:1.5rem;"></div>
      </div>
    </section>

    <!-- PROJECTS GRID -->
    <section class="portfolio-grid">
      {% for project in projects %}
      <article class="portfolio-item" data-service="{{ project.service }}" data-id="{{ project.slug }}">
        <div class="portfolio-img-wrap">
          <div class="portfolio-comparison">
            <div class="comparison-slider">
              <div class="comparison-img comparison-img-before"><img src="{{ project.before.src }}" alt="{{ project.before.alt }}" loading="lazy" {% if project.before.srcset %}srcset="{{ project.before.srcset | replace(', images/', ', /images/') | replace('//', '/') }}"{% endif %} sizes="(max-width: 768px) 100vw, 33vw"></div>
              <div class="comparison-img comparison-img-after"><img src="{{ project.after.src }}" alt="{{ project.after.alt }}" loading="lazy" {% if project.after.srcset %}srcset="{{ project.after.srcset | replace(', images/', ', /images/') | replace('//', '/') }}"{% endif %} sizes="(max-width: 768px) 100vw, 33vw"></div>
              <div class="comparison-slider-handle"><div class="comparison-divider"><div class="comparison-knob">‹ ›</div></div></div>
            </div>
          </div>
          <button class="expand-btn" aria-label="Toggle before and after photo" data-before="{{ project.before.src }}" data-after="{{ project.after.src }}"><svg class="icon"><use href="#icon-expand"/></svg></button>
        </div>
        {% set icon = project.category == 'Hardscaping' and 'bricks' or project.category == 'Landscaping' and 'leaf' or project.category == 'Carpentry' and 'square' or project.category == 'Exterior Care' and 'window' or 'fence' %}
        <h3><a href="/projects/{{ project.slug }}">{{ project.title }}</a></h3>
        <div class="project-meta">
          <span class="project-tag"><svg class="icon icon-sm"><use href="#icon-{{ icon }}"/></svg>{{ project.category }}</span>
          <span class="project-tag"><svg class="icon icon-sm"><use href="#icon-pin"/></svg>{{ project.location }}</span>
          <a href="/services/{{ project.service }}" class="project-meta-link">View Service →</a>
        </div>
        <a href="/projects/{{ project.slug }}" class="btn-primary">View Project</a>
      </article>
      {% endfor %}
    </section>

    <!-- STATS SECTION -->
    <section class="stats-strip">
      <div class="stats-row">
        <div class="stat-card">
          <span class="stat-number">12</span>
          <span class="stat-label">Projects</span>
        </div>
        <div class="stat-card">
          <span class="stat-number">8</span>
          <span class="stat-label">Years</span>
        </div>
        <div class="stat-card">
          <span class="stat-number">150+</span>
          <span class="stat-label">Happy Clients</span>
        </div>
      </div>
    </section>

    <!-- CTA SECTION -->
    <section class="cta-section">
      <h2 class="section-title">Ready to Transform Your Space?</h2>
      <p class="section-subtitle">Browse our portfolio and find the perfect fit for your property. Each project tells a story of transformation.</p>
      <a href="/contact" class="btn-primary">Get Inspired</a>
      <a href="/portfolio" class="btn-ghost">View All Projects</a>
    </section>

  </main>

  {% include "footer.njk" %}
  {% include "scripts.njk" %}

</body>
</html>